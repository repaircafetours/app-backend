info:
  name: Update visitor
  type: http
  seq: 6

http:
  method: PATCH
  url: http://localhost:8000/api/v1/visitors/1
  body:
    type: json
    data: |-
      {
        id: 1,
        notification: false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
