info:
  name: Add visitor
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/api/v1/visitors
  body:
    type: json
    data: |-
      {
        "title": "homme",
        "name": "Horgues",
        "surname": "Quentin",
        "zip_code": "370000",
        "city": "tours",
        "source": "",
        "notification": true,
        "email": "a@a.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
